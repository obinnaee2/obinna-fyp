package com.fyp.reconciliation_automation.repository;

import com.fyp.reconciliation_automation.entity.DebitSuccessful;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DebitSuccessfulRepository extends JpaRepository<DebitSuccessful, String> {
}
